# ===========================================
# AUTOLYTIQ PRODUCTION ENVIRONMENT
# ===========================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control
# ===========================================

# ===========================================
# DATABASE
# ===========================================
POSTGRES_USER=autolytiq_prod
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_DB=autolytiq

# ===========================================
# REDIS
# ===========================================
# Redis is internal only, no auth needed for internal network

# ===========================================
# JWT AUTHENTICATION
# ===========================================
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_GENERATE_WITH_OPENSSL_RAND_BASE64_64
JWT_ISSUER=autolytiq

# Token expiration times
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d

# ===========================================
# CORS / DOMAIN
# ===========================================
# Your production domain(s) - comma separated
ALLOWED_ORIGINS=https://app.yourdomain.com

# ===========================================
# EMAIL SERVICE (SMTP)
# ===========================================
# For production, use a reliable SMTP provider
# Examples: SendGrid, AWS SES, Mailgun, Postmark
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=YOUR_SENDGRID_API_KEY
FROM_EMAIL=noreply@yourdomain.com

# ===========================================
# SSL/TLS (Let's Encrypt)
# ===========================================
# Your domain for SSL certificate
DOMAIN=app.yourdomain.com
CERTBOT_EMAIL=admin@yourdomain.com

# ===========================================
# OPTIONAL: External Services
# ===========================================
# Sentry for error tracking
# SENTRY_DSN=https://xxx@sentry.io/xxx

# DataDog for monitoring
# DD_API_KEY=your_datadog_api_key

# AWS S3 for file uploads (vehicle images, documents)
# AWS_ACCESS_KEY_ID=xxx
# AWS_SECRET_ACCESS_KEY=xxx
# AWS_S3_BUCKET=autolytiq-uploads
# AWS_REGION=us-east-1
